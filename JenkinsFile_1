
pipeline{
    agent{
        docker { image 'alpine:latest'}
    }
    stages{
        stage('Test'){
            steps{
                sh 'whoami'
                sh 'docker version'
            }
        }
    }
}
